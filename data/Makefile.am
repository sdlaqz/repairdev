servicedir = $(libdir)/systemd/system
service_in_files = repairdev.service.in
service_DATA = $(service_in_files:.service.in=.service)

EXTRA_DIST = $(serivice_in_files)

CLEANFILES = $(serivice_DATA)

DISTCLEANFILES = $(serivice_DATA)
